## Focus group 1: uncertainty and sensitivity analysis for dynamic MFA

*Rick Lupton, June 2026*

The main notebooks are in the [students/](students/) subfolder.

Full versions, with example working code for the main exercises, are available for reference in the [full/](full/) subfolder.

Data files needed by the notebooks are in `data/`

### Python dependencies

Using `uv`, you can run `uv sync` in this folder to install the required dependencies in a new environment. Then run `uv run jupyter lab` to open Jupyter lab to access the notebooks.
